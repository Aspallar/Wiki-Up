<Page x:Class="WikiUpload.Pages.UploadedFilesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WikiUpload.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding Path=UploadViewModel, Source={StaticResource ServiceLocator}}"
      Title="UploadedFilesPage">

    <Grid>
        <Border Background="{StaticResource BackGroundColorBrush}" BorderThickness="4">
                <ListBox FontSize="{StaticResource FontSizeRegular}"
                         ItemsSource="{Binding UploadedFiles}"
                         Background="Transparent"
                         Focusable="False"
                         BorderThickness="0"
                         Style="{StaticResource ItemHighlight}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Button Style="{StaticResource FilenamePlainButton}"
                                Content="{Binding FileName, Mode=OneTime}"
                                Command="{Binding DataContext.LaunchFilePageCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=Page, AncestorLevel=1}}"
                                CommandParameter="{Binding}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

    </Grid>
</Page>
