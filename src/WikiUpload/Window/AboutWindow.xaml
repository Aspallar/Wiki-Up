<Window x:Class="WikiUpload.AboutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WikiUpload"
        mc:Ignorable="d"
        WindowStyle="ToolWindow"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Title="About Wiki-Up" Height="200" Width="320">
    
    <Grid Background="{StaticResource BackGroundColorBrush}">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" BorderThickness="0 0 0 1" BorderBrush="{StaticResource ButtonColorBrush}">
            <Grid >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="2*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">

                    <Image Source="{StaticResource LogoImage}" Width="64" Height="64" RenderTransformOrigin="0.5, 0.5">
                        <Image.Resources>
                            <Storyboard x:Key="Spin">
                                <DoubleAnimation
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                    From="0"
                                    To="360"
                                    Duration="0:0:1"/>
                            </Storyboard>
                        </Image.Resources>
                        <Image.Triggers>
                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                <EventTrigger.Actions>
                                    <BeginStoryboard Storyboard="{StaticResource Spin}" />
                                </EventTrigger.Actions>
                            </EventTrigger>
                        </Image.Triggers>
                        <Image.RenderTransform>
                            <RotateTransform/>
                        </Image.RenderTransform>
                    </Image>
                        
                </StackPanel>
                    
                <StackPanel Grid.Column="1" HorizontalAlignment="Center" Margin="20 20 20 0"
                        TextBlock.TextAlignment="Center"
                        TextBlock.FontFamily="{StaticResource LatoRegular}"
                        TextBlock.FontSize="{StaticResource FontSizeSmall}">
                    <TextBlock Text="Wiki-Up" FontSize="18" FontWeight="Bold" />
                    <TextBlock Text="{Binding VersionText}" Margin="0 8 0 0" />
                    <TextBlock Text="{Binding CopyrightText}" Margin="0 8 0 0"/>
                </StackPanel>
                    
            </Grid>
        </Border>

        <Button Grid.Row="1" Content="Ok" HorizontalAlignment="Center" Margin="0 0 10 0"
                Height="30" Width="120" FontSize="14"
                Command="{Binding CloseCommand}"/>

</Grid>

</Window>
